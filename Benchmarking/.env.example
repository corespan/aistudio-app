# Backend API origin. Required — the dev server throws on startup if unset.
# Point this at wherever your aistudio-server instance is reachable.
VITE_API_URL="http://localhost:8002"

# Optional: browser to open when the dev server starts (chrome, firefox,
# safari, or a path to a binary). Leave unset to use the system default.
BROWSER=chrome
